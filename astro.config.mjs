// @ts-check
import { defineConfig } from 'astro/config';

// Static site (default output) — deploys to Vercel with zero config.
export default defineConfig({
  site: 'https://www.ideabio.org.au',
});
